module.exports = {
    inputFilePath: './data/access.log',  // Path to the input log file
    outputFilePath: './dist/parsed_logs.csv',  // Path for saving the output
    errorLogPath: './logs/errors.log',  // Path for error logging
    delimiter: ' ',  // Default delimiter for the log file (adjust based on your log format)
  };
  